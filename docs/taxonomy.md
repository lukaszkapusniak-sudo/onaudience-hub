# `taxonomy.json` — tag taxonomy data

**Path:** [`www/hub/taxonomy.json`](../www/hub/taxonomy.json)

JSON data used for **tagging / taxonomy** features in the hub (paired with [`TAG_RULES`](config.md) and list rendering in [`list.md`](list.md)).

Update when marketing or product taxonomy changes; keep in sync with any Supabase tables that mirror tags if applicable.
